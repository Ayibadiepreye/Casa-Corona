# Frontend env (copied to apps/web/.env for local dev)
# In Vercel, set this as a project env var

# URL of the API (NO trailing slash, include /api/v1 path)
VITE_API_URL=http://localhost:5000/api/v1
